  <%= f.label :title %>
  <%= f.text_field :title %><br><br>
 
  <%= f.label "Select Categories:" %>
  <%= f.collection_check_boxes(:category_ids, current_user.categories.uniq, :id, :name) %><br>
  <p>-- OR --</p>
  <%= f.fields_for :categories, @task.categories.build do |category| %>
    <%= category.text_field :name%>
  <% end %><br><br>


  <%= f.label :description %><br>
  <%= f.text_area :description, size: "30x5" %><br>
  <%= f.submit %>
